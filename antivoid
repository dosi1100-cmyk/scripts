if _G.AntiVoidT == true then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "[ ⚠️AntiVoid ]",
        Text = "이미 켜져있습니다.",
        Duration = 2
    })
    return
end

_G.AntiVoidT = true

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local existingPlatform = workspace:FindFirstChild("AntiVoidPlatform")
if existingPlatform then
    existingPlatform:Destroy()
end

local platform = Instance.new("Part")
platform.Name = "AntiVoidPlatform"
platform.Size = Vector3.new(2048, 3, 2048)
platform.Position = Vector3.new(0, -280, 0)
platform.Anchored = true
platform.Transparency = 1
platform.CanCollide = true
platform.Parent = workspace

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "[ 🟢AntiVoid ]",
    Text = "Anti Void가 켜졌습니다.",
    Duration = 2
})

workspace.FallenPartsDestroyHeight = -50000

task.spawn(function()
    while _G.AntiVoidT do
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                if platform and platform.Parent then
                    platform.Position = Vector3.new(hrp.Position.X, -280, hrp.Position.Z)
                end

                if hrp.Position.Y < -330 then
                    hrp.CFrame = CFrame.new(0, -2, 0)
                    
                    hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                    hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                    
                    hrp.Anchored = true
                    task.wait(0.03)
                    hrp.Anchored = false
                end
            end
        end
        task.wait()
    end
end)
