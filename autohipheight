local Players = game:GetService("Players")
local TARGET_PLACE_ID = 6961824067

if game.PlaceId ~= TARGET_PLACE_ID then
	return
end

local TARGET_HIP_HEIGHT = 2
local CHECK_INTERVAL = 0.15

local R6_LEG_PARTS = {
	["Left Leg"] = true,
	["Right Leg"] = true,
}

local function allLegsGone(character)
	for partName in pairs(R6_LEG_PARTS) do
		local part = character:FindFirstChild(partName)
		if part and part:IsA("BasePart") then
			return false
		end
	end
	return true
end

local function hasLegs(character)
	for partName in pairs(R6_LEG_PARTS) do
		local part = character:FindFirstChild(partName)
		if part and part:IsA("BasePart") then
			return true
		end
	end
	return false
end

local function watchCharacter(character)
	local humanoid = character:WaitForChild("Humanoid", 10)
	if not humanoid then
		return
	end

	if humanoid.RigType ~= Enum.HumanoidRigType.R6 then
		return
	end

	local antiExplodeFired = false
	local hadLegs = hasLegs(character) -- 처음에 다리가 있었는지 확인

	task.spawn(function()
		while character.Parent do
			if humanoid.Parent and humanoid.Health > 0 then
				local legsNowGone = allLegsGone(character)

				-- 다리가 있다가 없어진 상태여야만 실행
				if hadLegs and legsNowGone then
					if humanoid.HipHeight ~= TARGET_HIP_HEIGHT then
						humanoid.HipHeight = TARGET_HIP_HEIGHT
					end

					if not antiExplodeFired then
						antiExplodeFired = true
						loadstring(game:HttpGet('https://raw.githubusercontent.com/dosi1100-cmyk/scripts/refs/heads/main/antiexplode'))()
					end
				end

				-- 다리가 있는 상태라면 hadLegs 유지
				if not legsNowGone then
					hadLegs = true
				end
			end

			task.wait(CHECK_INTERVAL)
		end
	end)
end

local function onPlayerAdded(player)
	player.CharacterAdded:Connect(function(character)
		watchCharacter(character)
	end)

	if player.Character then
		watchCharacter(player.Character)
	end
end

Players.PlayerAdded:Connect(onPlayerAdded)

for _, player in ipairs(Players:GetPlayers()) do
	onPlayerAdded(player)
end
